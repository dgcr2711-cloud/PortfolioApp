# Dependência extra, só necessária se você configurar a sincronização com
# o app do celular (ver README_MOBILE.md). Sem isso instalado, o app do PC
# funciona 100% normalmente — a sincronização é simplesmente ignorada.
#
# Instale com: pip install -r requirements-mobile.txt
#
# Nota (2026-08-30): desde que o dashboard passou a ser hospedado no
# Streamlit Community Cloud, o firebase-admin também foi adicionado ao
# requirements.txt principal (obrigatório lá). Este arquivo continua
# existindo só como referência/instalação manual isolada, se precisar.

firebase-admin>=6.5
