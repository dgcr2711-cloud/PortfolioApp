"""
Arquivo de configuração do pytest.

Só precisa existir aqui na raiz do projeto (não precisa de nenhum conteúdo
especial) para que o pytest coloque esta pasta no caminho de importação do
Python automaticamente — assim os testes em tests/ conseguem fazer
`from core import calculations` sem precisar instalar o projeto como pacote
nem mexer em nenhuma variável de ambiente.
"""
