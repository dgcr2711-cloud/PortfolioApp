streamlit>=1.36
yfinance>=0.2.40
requests>=2.31
pandas>=2.0
plotly>=5.20
pdfplumber>=0.11
openpyxl>=3.1
# firebase-admin precisa estar aqui (e não só em requirements-mobile.txt)
# porque o Streamlit Community Cloud só instala o que está neste arquivo
# principal — ele não roda o "Iniciar App.bat" nem sabe instalar
# bibliotecas condicionalmente. Adicionado em 2026-08-30 depois que o
# dashboard hospedado quebrou com "ModuleNotFoundError: No module named
# 'firebase_admin'" ao tentar sincronizar com o Firestore.
firebase-admin>=6.5
# streamlit-authenticator: tela de login (e-mail/senha) que protege o
# dashboard hospedado (core/auth.py) — sem senha, hoje o link do site fica
# protegido só por ser "secreto". Precisa estar aqui (não em
# requirements-dev.txt) pelo mesmo motivo do firebase-admin acima: o
# Streamlit Community Cloud só instala o que está neste arquivo.
streamlit-authenticator>=0.4
